CREATE TABLE Profile (PROFILE_ID VARCHAR DEFAULT(''), PROFILE_NAME VARCHAR DEFAULT(''), RINGTONE_PATH VARCHAR DEFAULT(''), WALLPAPER_PATH VARCHAR DEFAULT(''), VOLUME VARCHAR DEFAULT(''), LATITUDE VARCHAR DEFAULT(''), LONGITUDE VARCHAR DEFAULT(''), ADDRESS VARCHAR DEFAULT(''), PROFILE_MODE VARCHAR DEFAULT(''));
CREATE TABLE Alert (ALERT_ID VARCHAR DEFAULT(''), LAT VARCHAR DEFAULT(''), LANG VARCHAR DEFAULT(''));